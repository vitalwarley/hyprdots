{
  "custom/claude": {
    "exec": "/home/warley/life/2-areas/dev-tools/hyprdots/waybar/scripts/claude-usage.sh",
    "return-type": "json",
    "interval": 30,
    "tooltip": true,
    "max-length": 30,
    // Anthropic/Claude doesn't have a Nerd Font glyph; using nf-md-creation
    // (sparkle) as it's the widely-adopted "AI" indicator across linux setups.
    // Swap for nf-md-robot (󰊩) or any preferred glyph.
    "format": "  {}"
  }
}

